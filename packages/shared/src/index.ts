// Placeholder for shared contracts (Zod schemas, DTO types) used by both
// apps/api and apps/mobile. Starting in M2, request/response schemas for
// each resource will be added here so both apps import the same definitions
// instead of maintaining two copies.

export const SHARED_PACKAGE_ID = "@taskflow/shared" as const;
